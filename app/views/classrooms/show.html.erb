Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to name all students in the class:
<% @classroom.students.each do |s|%>
  <%= render partial: "students/student", locals: {student: s} %>
<% end %>
